# GrandMA2 Hub

An Electron application for managing GrandMA2 hubs and related tools.

## Development

1. Run `npm install` to install dependencies.
2. Run `npm start` (or the equivalent script) to launch the Electron application.

## Features
- TBD
